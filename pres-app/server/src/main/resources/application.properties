## Use Railway's provided PORT env var when present, otherwise default to 8080
server.port=${PORT:8080}

## Bind to all interfaces inside the container so external requests are accepted
server.address=0.0.0.0
